Let's verify what CHAAPP/CNTAPP actually control before touching them — the existing docs label them "speed/attack timing" but that's an unverified guess, and the French naming (CHAnce APParition / CoNTe APParition) reads much more like spawn chance/count, which is exactly what we need for "more variable spawn rates."

Confirmed — this is a real, previously-mislabeled finding: BOSSX, BOSSX2/3, all RATMANLORD* (unique named bosses) consistently have CNTAPP=1, while common trash (CHICKEN, FLY5, SPIDER1) sit at 3-4, and the field ranges all the way up to 80 for swarm-type monsters. That's a spawn-count field, not "attack timing" as the notes guessed. This is exactly the lever for spawn variability.

Now let's check NIVEAUX.TXT (French for "LEVELS.TXT") — if it controls the XP curve, that's the actual lever for "more grinding needed per land."

NIVEAUX.TXT doesn't look like an XP curve at all — no header, values only span 23-41 across 35 levels, no exponential growth. XP-to-level is very likely hardcoded in the EXE rather than in a data table, so that's not a lever we have right now (touching it would mean new EXE reverse-engineering, a separate undertaking from what's below).

Given that, the achievable, concrete levers for "more grinding, more variable spawns, balanced curve redesign" are: monster stat scaling by level, a matching-but-lagging item power curve, and spawn count/chance variance. Let's check whether CHAAPP/CNTAPP/speed fields are already located in the binary .DAT format (since that's what the game actually reads at runtime) — the existing "randomize enemy speeds" feature already touches SPEED/ATTFRE/ATTSPD there.

Important finding: randomizeMonsterSpeeds only edits the TXT table, not the binary .DAT — meaning under the new DAT-first architecture, that feature currently has zero effect on actual gameplay unless the optional TXT/MTF path is enabled (which still carries the old clamp bug). Let's check whether the binary DAT patcher already knows the offsets for CHAAPP/CNTAPP/SPEED/ATTFRE/ATTSPD, since that's what actually needs editing.

This is genuinely new territory — the DAT patcher never mapped SPEED/ATTFRE/CHAAPP/CNTAPP/ATTSPD at all. But there's a strong pattern to test: the known fields sit at offsets 68/72/76/80/84/88 (each 4 bytes apart). If the struct just continues sequentially, the remaining fields should be at 92/96/100/104/108. Let's test that against real data, cross-checking each DAT record's values against the same monster's known values in MONSTER.TXT.

The sequential-continuation guess was wrong. Let's do a proper brute-force scan — check every 2-byte-aligned offset in the record against the known field values, the same way the original LMIN/LMAX/AC/DMIN/DMAX offsets must have been found.

Excellent — very high confidence hits: CHAAPP@214 (99.0%), CNTAPP@216 (99.0%, right next to CHAAPP as expected), ATTSPD@222 (100.0%), ATTFRE@126 (95.3%), SPEED@534 (95.8%). This confirms CHAAPP/CNTAPP genuinely exist in the runtime DAT, not just the optional TXT export. Now I can build the actual redesign against the file the game actually reads.

Let's check patchItemDat's current fields before designing the matching item curve.

One issue surfaced in that test: APOTHICAIRE (a shopkeeper NPC, in the protected list) had its combat stats altered anyway. This is a pre-existing bug, not something I introduced — isProtectedMonster was never actually checked before the legacy shuffle/range-roll branches, only in my new curve branch. Since I'm already touching this exact code, let's fix it properly.